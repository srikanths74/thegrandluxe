import type { Metadata } from "next";
import { Inter, Cormorant_Garamond } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
  weight: ["300", "400", "500", "600", "700"],
});

const cormorant = Cormorant_Garamond({
  variable: "--font-cormorant",
  subsets: ["latin"],
  display: "swap",
  weight: ["300", "400", "500", "600", "700"],
  style: ["normal", "italic"],
});

export const metadata: Metadata = {
  title: "Grand Luxe Hotel & Dining — Luxury Suites & In-Room Gourmet Service",
  description:
    "Reserve lavish oceanfront suites and enjoy 24/7 world-class in-room dining at Grand Luxe Hotel. 5-star Michelin hospitality, keyless check-in, and butler service.",
  keywords: "luxury hotel, in-room dining, oceanfront suites, penthouse, gourmet room service, 5 star resort",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${inter.variable} ${cormorant.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
